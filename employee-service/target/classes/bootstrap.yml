server:
  port: 10002
spring:
  application:
    name: employee-service
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/