spring:
  application:
    name: api-gateway-service
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
security:
  jwt:
    public-key: classpath:public.txt