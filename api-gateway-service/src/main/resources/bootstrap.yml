spring:
  application:
    name: api-gateway-service
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
server:
  port: 8765
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka