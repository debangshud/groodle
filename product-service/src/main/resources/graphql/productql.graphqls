type Query {
  products(count: Int):[Product]
  productById(id: ID): Product
}

type Product {
  id: ID!,
  name: String,
  description: String,
  manufacturer: Manufacturer
}

type Manufacturer {
  id: ID!,
  name: String
}