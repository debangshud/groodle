#server:
#  port: 10001
spring:
  application:
    name: customer-service
  cloud:
    config:
      discovery:
        service-id: config-service
        enabled: true
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA_SERVER:http://discovery:8761/eureka/}