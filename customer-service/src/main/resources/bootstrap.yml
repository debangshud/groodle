server:
  port: 10001
spring:
  application:
    name: customer-service
  cloud:
    config:
      discovery:
        service-id: spring-cloud-config-service
        enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/